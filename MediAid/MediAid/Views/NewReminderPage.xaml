<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="MediAid.Views.NewReminderPage"
		Title="New Reminder">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Save" Clicked="Save_Clicked" />
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        <ScrollView >
            <StackLayout Spacing="20" Padding="15">
            <Label Text="Reminder Name" FontSize="Medium" />
            <Entry Text="{Binding Reminder.Name, Mode=TwoWay}" FontSize="Small" />
            <Label Text="Message" FontSize="Medium" />
            <Editor Text="{Binding Reminder.Message, Mode=TwoWay}" FontSize="Small" Margin="0" />
            <Button Text="{Binding ButtonText}" Clicked="Record_Reminder" BackgroundColor="{StaticResource Primary}" TextColor="White">
            </Button>
            
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>
